# 充值、提现与白名单接入（2026-09-18）

## 当前状态

- 游戏： https://sheep-fortune-game.lingolayer.workers.dev/
- 管理： https://sheep-fortune-game.lingolayer.workers.dev/admin/
- 白名单： https://sheep-fortune-game.lingolayer.workers.dev/admin/whitelist/
- BSC 代币：`0xaf4d973fceacba24467c01ca8c8dd0e820b18732`
- 已绑定托管合约：`0x8Fd45A2B7582fe5bEc61b7aF959C69714baFA1B5`
- 项目管理员：`0x0e6cFe55b8F1AC4e1E0377AD2b7078716aB06f34`
- 后台签名人公钥：`0xbd0Ec3c9DBE22beb6C97581888cf454416329ceA`
- 链上实际单笔提现上限：100万枚；单批销毁上限：10万枚。
- 公开充提关闭，管理员钱包验收开启。管理员通过同一个游戏页面完成充值、提现及游戏；其他地址没有验收权限。
- 已应用 D1 迁移0007，开启每分钟奖池与充提核对。
- 初始注资200万枚已入账。核对时游戏税等直接收入约3.758万枚，可用奖池约203.758万枚；实际以后台实时结果为准。
- 当前白名单为空，未擅自配置任何地址或新分布。

## 充值

“我的账户 → 充值”：输入币数，钱包确认精确额度授权（已有足够授权则跳过），再确认 `deposit`。按 `Deposited` 实际到账、正确代币/钱包/托管合约、至少20区块确认入账。普通直接转币不计为个人充值。

交易提交后保存交易哈希，登记后台待确认任务，网页每15秒核对，后台每分钟补充核对。刷新或重新打开可恢复；若本机存储丢失，可用自己的充值交易哈希核对。失败记录会说明原因，同一交易重复核对不会重复入账。链上RPC故障会保持待确认，不会伪造到账。

## 提现

“我的账户 → 提现”：输入数量，查看手续费与预计到账，确认冻结，再由钱包执行一次性提现凭证。当前普通提现手续费0，白名单提现也为0，没有擅自启用盈利提现5%。链上Gas由用户钱包支付。

凭证固定收款人、币数、BSC网络、托管地址、有效期与签名人版本；有效期1小时。无法冒用其他玩家凭证或转到别人的地址。重复提交不重复付款，冻结余额不能再次下注或提现。实际到账核对完成后自动清除冻结；未执行且确认过期/撤销的申请自动退回余额。钱包取消操作不立即解冻，避免仍有效的凭证被重复花费。

BSC公共节点可能关闭 `eth_getLogs`。程序根据托管合约记录的执行区块，找到页面发起的直接交易，再校验交易回执和事件；无需遍历历史日志。外部合约中继仍使用事件查询，因此运营如引入第三方中继需配置支持日志的RPC。

## 白名单与管理

项目管理员签名登录后，“我的账户”显示运营管理入口。管理页提供奖池、充值和提现记录，白名单页可新增、编辑和停用地址。管理员地址由服务端 `ADMIN_WALLET` 决定，不能由前端请求伪造；管理员钱包更换时需同步更新此配置。

每个地址有9档概率，最多两位小数、合计100%；启用后新一局使用该配置，并免游戏提现手续费。停用恢复普通规则，已结算游戏与已申请提现保留原快照。客户端不决定中奖结果。编辑采用版本校验；不确定的保存重试复用原操作编号。

管理支持钱包会话（POST同源验证）或服务端OPS密钥。密钥仅用于备用，不能写入前端配置或Git。普通账户、其他代币账户和跨站请求不能执行管理写操作。退出会撤销服务端登录会话，旧Cookie不能重新进入后台。

## 奖池核对

`POOL_FUND_SYNC_ENABLED=true` 时，根据确认区块的累计 `totalFunded`，扣除已手动入账部分和自动入账检查点，仅入账增量；游戏税等 `directPoolIncome` 独立核对。重复任务或并发不重复增加奖池。用户充值不被计入可支出的游戏奖池。累计自动注资启用后拒绝再次按注资哈希重复入账。

`DEPOSIT_START_BLOCK=122584076` 是本次充值开放检查点，不是合约部署区块。设置前确认托管合约累计用户充值和提币均为0。已经完成的200万注资通过累计计数核对，不受该充值起始区块影响。

后台每次补充处理最多2笔待确认充值和2笔待完成提现，页面会同时核对自己的交易。该任务只读取链上信息和更新账本，不代替用户发送交易或花Gas。大量离线待处理任务可能排队；可用管理页主动核对。奖池读取失败不会阻止本轮充提核对尝试。

## 验证与仍需完成的事项

- 应用与账本测试92项通过；合约测试11项通过（含256轮随机化守恒测试）。
- 新增覆盖管理员签名会话、跨站与跨代币隔离、名单增停、授权复用、暂停保护、后台充提核对、过期返还、累计注资防重和公共节点禁用日志的兼容处理。
- 手机390×844页面使用本地真实EVM完成登录、充值500、余额10000→10500、提现300→10200、白名单保存；无脚本错误及横向溢出。
- 现有BSC合约的本地分叉完成充值1000、足额提现100、注资100、卖出1000入池税10、原生销毁100。分叉签名人是临时密钥，未广播主网交易。
- 线上链上余额与后台奖池一致，双边账本无不匹配。
- 尚待项目钱包实际充值10枚再提现10枚完成主网验收；当前不代表所有用户已经开放。
- 普通净返还77%、销毁9.3%、直推15%、间推5%均未修改。两级佣金都发放时，每单位下注理论奖池净变化为−6.3%；无上级时为+13.7%。公开开放前应确认按既有经济参数运营。

验收后由运营决定公开开放：将 `LIVE_PAYMENTS_ENABLED=true`、`PAYMENTS_VALIDATION_ENABLED=false` 后部署。保留 `VAULT_ADDRESS`、起始区块、ADMIN_WALLET、签名secret与数据库，不能换资产继承旧余额。关闭开关会同时暂停新的真实游戏和充提，已有凭证仍以链上有效期为准。
